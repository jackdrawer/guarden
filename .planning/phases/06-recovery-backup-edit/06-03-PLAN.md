---
phase: 06-recovery-backup-edit
plan: 03
type: execute
depends_on: ["06-01"]
files_modified:
  - lib/services/backup_service.dart
  - lib/screens/settings/settings_screen.dart
  - lib/providers/settings_provider.dart
requirements:
  - R8
must_haves:
  truths:
    - "Backup export is encrypted and versioned."
    - "Restore supports decrypt + schema validation + dry-run before apply."
    - "User-facing backup actions are available from Settings."
  artifacts:
    - path: "lib/services/backup_service.dart"
      provides: "Backup export/import core"
    - path: "lib/screens/settings/settings_screen.dart"
      provides: "Backup action entry points"
---

<objective>
Deliver encrypted local backup/restore MVP with safe restore process.
</objective>

<tasks>
- Implement `BackupService` with:
  - export payload schema (`version`, `createdAt`, `data`, `checksum`)
  - encrypt/decrypt helpers (PBKDF2 + AES-GCM)
  - validation and dry-run summary model
- Add settings actions:
  - "Yedek Al"
  - "Yedekten Geri Yukle"
- Add restore confirmation step after dry-run report.
- Add robust error handling for wrong passphrase/corrupt file.
</tasks>

<verification>
- `flutter analyze` passes.
- Unit tests for:
  - encrypt/decrypt roundtrip
  - checksum mismatch
  - version mismatch rejection
- Manual test:
  1. Export data.
  2. Clear app state or use test vault.
  3. Restore from file.
  4. Verify records reappear.
</verification>

<output>
Create `06-03-SUMMARY.md` after completion.
</output>
