---
phase: 06-recovery-backup-edit
plan: 02
type: execute
depends_on: ["06-01"]
files_modified:
  - lib/router.dart
  - lib/screens/bank_accounts/bank_account_detail_screen.dart
  - lib/screens/bank_accounts/bank_account_form_screen.dart
  - lib/screens/subscriptions/subscription_detail_screen.dart
  - lib/screens/subscriptions/subscription_form_screen.dart
  - lib/screens/web_passwords/web_password_detail_screen.dart
  - lib/screens/web_passwords/web_password_form_screen.dart
requirements:
  - R9
must_haves:
  truths:
    - "Each detail screen edit button opens a real edit form."
    - "Form screens support both create and edit modes."
    - "Edit updates persist encrypted fields correctly."
  artifacts:
    - path: "lib/router.dart"
      provides: "Edit routes for bank/sub/web"
    - path: "lib/screens/*/*_form_screen.dart"
      provides: "Mode-aware create/edit forms"
---

<objective>
Complete edit flows for all credential modules with a shared mode-aware form pattern.
</objective>

<tasks>
- Add routes:
  - `/edit-bank/:id`
  - `/edit-subscription/:id`
  - `/edit-web-password/:id`
- Add optional form params (`itemId` or `existingItem`) for edit mode.
- Prefill form data by decrypting stored fields where required.
- Switch save action to `add*` for create and `update*` for edit.
- Replace placeholder snackbars in detail screens with route navigation.
- Ensure travel-protection toggle can be edited and persisted.
</tasks>

<verification>
- `flutter analyze` passes.
- Form-mode tests validate create/edit branches.
- Manual flow per module:
  1. Open detail.
  2. Tap edit.
  3. Change value.
  4. Save.
  5. Confirm detail reflects update.
</verification>

<output>
Create `06-02-SUMMARY.md` after completion.
</output>
