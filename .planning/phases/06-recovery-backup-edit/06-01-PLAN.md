---
phase: 06-recovery-backup-edit
plan: 01
type: execute
depends_on: []
files_modified:
  - lib/services/secure_storage_service.dart
  - lib/providers/auth_provider.dart
  - lib/screens/auth/login_screen.dart
  - lib/screens/onboarding/welcome_screen.dart
  - lib/router.dart
requirements:
  - R7
must_haves:
  truths:
    - "Seed phrase is persisted only as encrypted blob in secure storage."
    - "User can recover vault with seed phrase and set a new master password."
    - "Panic flow and recovery flow are route-consistent."
  artifacts:
    - path: "lib/services/secure_storage_service.dart"
      provides: "Seed phrase storage API"
    - path: "lib/providers/auth_provider.dart"
      provides: "recoverWithSeedPhrase workflow"
    - path: "lib/screens/auth/recovery_screen.dart"
      provides: "Recovery UI and validation"
    - path: "lib/router.dart"
      provides: "Recovery route wiring"
---

<objective>
Implement recovery foundation end-to-end: secure seed persistence, recovery screen, and auth-notifier recovery logic.
</objective>

<tasks>
- Add encrypted seed storage methods to `SecureStorageService`.
- During setup, persist encrypted seed recovery blob.
- Implement `recoverWithSeedPhrase(seedPhrase, newPassword)` in `AuthNotifier`.
- Add `RecoveryScreen` and route `/recovery`.
- Add "Master sifremi unuttum" entry points from welcome/login surfaces.
- Ensure panic-reset state naturally leads user toward recovery path.
</tasks>

<verification>
- `flutter analyze` passes.
- Unit tests for recovery success/failure paths pass.
- Manual flow:
  1. Setup vault.
  2. Trigger panic.
  3. Recover with seed phrase.
  4. Login with new master password.
</verification>

<output>
Create `06-01-SUMMARY.md` after completion.
</output>
