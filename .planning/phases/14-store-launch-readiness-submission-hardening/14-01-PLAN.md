---
phase: 14-store-launch-readiness-submission-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/build.gradle.kts
  - android/app/src/main/AndroidManifest.xml
  - lib/services/ad_service.dart
  - lib/main.dart
  - lib/services/telemetry_service.dart
autonomous: true
requirements:
  - launch-android
  - launch-store-docs

must_haves:
  truths:
    - "Android release builds never fall back to debug signing and fail early when production signing input is missing"
    - "Release Android manifest advertises only launch-approved capabilities and uses production-safe AdMob configuration"
    - "Launch build config keeps ads and telemetry behind explicit release configuration instead of implicit test defaults"
  artifacts:
    - path: "android/app/build.gradle.kts"
      provides: "Fail-closed Android release signing and reproducible release build configuration"
      contains: "throw GradleException"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "Store-facing Android permissions and production AdMob app metadata"
      contains: "com.google.android.https://file+.vscode-resource.vscode-cdn.net/c%3A/Users/turga/.antigravity/extensions/openai.chatgpt-0.4.79-win32-x64/webview/gms.ads.APPLICATION_ID"
    - path: "lib/services/ad_service.dart"
      provides: "Production/test ad unit selection driven by explicit launch configuration"
      contains: "String.fromEnvironment"
    - path: "lib/main.dart"
      provides: "Explicit bootstrap for release-only telemetry and launch environment validation"
      contains: "String.fromEnvironment"
  key_links:
    - from: "lib/services/ad_service.dart"
      to: "android/app/src/main/AndroidManifest.xml"
      via: "runtime ad units align with native AdMob app ID"
      pattern: "APPLICATION_ID|String.fromEnvironment"
    - from: "lib/main.dart"
      to: "lib/services/telemetry_service.dart"
      via: "bootstrap gates telemetry on explicit launch config"
      pattern: "Telemetry|String.fromEnvironment"
---

<objective>
Harden Android release configuration so Play Store artifacts are signed correctly, production-safe, and reproducible from a clean release path.

Purpose: Close the Android-side blockers identified in launch review: debug signing fallback, test AdMob configuration, manifest capability drift, and non-deterministic release setup.

Output: Android release config that fails closed without signing secrets, loads production launch values explicitly, and supports a documented `flutter build appbundle --release` path.
</objective>

<execution_context>
@C:/Users/turga/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/turga/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-store-launch-readiness-submission-hardening/14-CONTEXT.md
@.planning/phases/14-store-launch-readiness-submission-hardening/14-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fail closed on Android release signing</name>
  <files>android/app/build.gradle.kts</files>
  <action>
Update `android/app/build.gradle.kts` so release builds cannot silently sign with the debug keystore:

- Remove the current fallback from release signing to `signingConfigs.getByName("debug")`.
- Introduce an explicit guard that throws a clear Gradle error when `key.properties` or required release signing keys are missing.
- Keep debug/profile behavior unchanged so local development still works.
- Ensure the release path remains compatible with Flutter's standard `flutter build appbundle --release` command.

Why: A password manager cannot ship with a release pipeline that accidentally produces debug-signed output.
  </action>
  <verify>
    <automated>cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && dart analyze lib test</automated>
    <manual>Run `flutter build appbundle --release` on a machine with valid `key.properties` and confirm the build fails with a configuration error when signing data is intentionally removed.</manual>
  </verify>
  <done>
- Release build no longer falls back to debug signing
- Missing signing inputs produce a clear hard failure
- Debug/profile builds still work for development
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean Android store manifest and launch config</name>
  <files>android/app/src/main/AndroidManifest.xml, lib/services/ad_service.dart, lib/main.dart, lib/services/telemetry_service.dart</files>
  <action>
Clean up Android launch configuration to match what will actually be submitted:

- Remove `com.android.vending.BILLING` from the release manifest unless a real Play Billing integration is present in code and store declarations.
- Replace test AdMob app ID and ad unit defaults with explicit production configuration, ideally via `--dart-define` or a small launch config abstraction.
- Ensure release bootstrapping does not silently enable test/demo monetization values.
- Audit telemetry bootstrap so Sentry/Firebase behavior is explicit and can be disclosed confidently for store review.
- Keep safe local defaults for development, but make release configuration intentional and inspectable.

Why: Store metadata, Data safety disclosures, and release binaries must reflect the real runtime surface.
  </action>
  <verify>
    <automated>cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && dart analyze lib test</automated>
    <automated>cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && rg -n "com.android.vending.BILLING|ca-app-pub-3940256099942544" android lib</automated>
  </verify>
  <done>
- Android manifest contains only launch-approved permissions/capabilities
- Test AdMob identifiers are removed from release-facing config
- Ads and telemetry are driven by explicit launch values
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove Android release path is reproducible</name>
  <files>android/app/build.gradle.kts, android/app/src/main/AndroidManifest.xml, lib/services/ad_service.dart, lib/main.dart</files>
  <action>
After config hardening, verify the Android release path end-to-end:

- Resolve current release build blockers on the local release machine, including cmdline-tools/licenses or NDK symbol-strip mismatches if they still block bundle creation.
- Re-run the release build using the hardened config and confirm the generated `.aab` comes from the intended release path.
- Capture the exact commands and preconditions needed for future release operators.
- If a blocker is external to the repo, document it explicitly rather than leaving the pipeline in an ambiguous state.

Why: "It built once" is not enough. This phase needs a repeatable Android submission path.
  </action>
  <verify>
    <automated>cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && flutter build appbundle --release</automated>
  </verify>
  <done>
- Android App Bundle builds successfully from the intended release path, or remaining external blockers are explicitly documented
- Release build command and prerequisites are captured for final launch checklist
  </done>
</task>

</tasks>

<verification>
Android launch verification:
```bash
cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && dart analyze lib test
cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && rg -n "com.android.vending.BILLING|ca-app-pub-3940256099942544" android lib ios
cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && flutter build appbundle --release
```

Manual checks:
- [ ] Release signing fails closed when `key.properties` is missing
- [ ] Release manifest no longer declares unsupported billing if billing is not shipped
- [ ] Final Android artifact uses production launch configuration rather than Google demo IDs
</verification>

<success_criteria>
- Android release pipeline is fail-closed and reproducible
- Play-facing manifest/config matches actual launch capabilities
- Release monetization and telemetry values are explicit and policy-auditable
- `flutter build appbundle --release` succeeds or leaves only documented external prerequisites
</success_criteria>

<output>
After completion, create `.planning/phases/14-store-launch-readiness-submission-hardening/14-01-SUMMARY.md`
</output>
