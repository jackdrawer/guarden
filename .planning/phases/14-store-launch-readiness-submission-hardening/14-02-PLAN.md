---
phase: 14-store-launch-readiness-submission-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/Runner.xcodeproj/project.pbxproj
  - ios/Runner/Info.plist
  - ios/Runner/AppDelegate.swift
  - ios/Podfile
  - ios/Runner/FIREBASE_SETUP.md
autonomous: true
requirements:
  - launch-ios
  - launch-store-docs

must_haves:
  truths:
    - "iOS project uses production bundle identity instead of placeholder identifiers"
    - "Face ID, Google Sign-In, and AdMob metadata required for archive/review are present in the iOS project"
    - "iOS launch prerequisites that cannot be completed on Windows are documented as explicit manual archive/signing steps"
  artifacts:
    - path: "ios/Runner.xcodeproj/project.pbxproj"
      provides: "Production bundle identifier and iOS build settings aligned with launch target"
      contains: "PRODUCT_BUNDLE_IDENTIFIER"
    - path: "ios/Runner/Info.plist"
      provides: "Face ID usage description, Google Sign-In URL handling metadata, and production AdMob app ID"
      contains: "NSFaceIDUsageDescription"
    - path: "ios/Runner/AppDelegate.swift"
      provides: "Explicit iOS bootstrap/callback handling for launch-critical plugins when required"
      contains: "application"
    - path: "ios/Podfile"
      provides: "Restored CocoaPods integration surface for reproducible iOS dependency install"
      min_lines: 20
  key_links:
    - from: "ios/Runner/Info.plist"
      to: "lib/services/biometric_service.dart"
      via: "Face ID prompt metadata matches biometric auth flow"
      pattern: "NSFaceIDUsageDescription|authenticate"
    - from: "ios/Runner/Info.plist"
      to: "lib/services/google_drive_backup_service.dart"
      via: "Google Sign-In URL configuration supports Drive auth flow"
      pattern: "CFBundleURLTypes|GoogleSignIn|GoogleSignIn"
---

<objective>
Bring the iOS project to archive-ready configuration with production identity, required biometric/sign-in metadata, and a documented manual submission path.

Purpose: Close iOS blockers found in review: placeholder bundle ID, missing Face ID usage description, incomplete Google Sign-In setup, missing Podfile, and absent archive prerequisites.

Output: iOS project files configured for production identity and review metadata, plus a clear macOS-side archive/signing checklist.
</objective>

<execution_context>
@C:/Users/turga/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/turga/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-store-launch-readiness-submission-hardening/14-CONTEXT.md
@.planning/phases/14-store-launch-readiness-submission-hardening/14-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace placeholder iOS identity and restore project prerequisites</name>
  <files>ios/Runner.xcodeproj/project.pbxproj, ios/Podfile</files>
  <action>
Make the iOS project structurally launch-ready:

- Replace placeholder `com.example.guarden` identifiers in `project.pbxproj` with the real production bundle identifier agreed for launch.
- Restore or generate a standard `ios/Podfile` if it is missing so CocoaPods-based Flutter plugin integration is reproducible on macOS.
- Keep the resulting project compatible with standard Flutter/iOS build tooling rather than a one-off local Xcode state.
- Document any external signing/team values that still must be supplied outside the repo.

Why: App Store submission starts with a valid iOS app identity and a reproducible dependency installation path.
  </action>
  <verify>
    <automated>cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && rg -n "com\\.example\\.guarden|PRODUCT_BUNDLE_IDENTIFIER" ios/Runner.xcodeproj/project.pbxproj ios</automated>
    <manual>On macOS, run `pod install` in `ios/` and confirm Runner workspace opens without missing dependency errors.</manual>
  </verify>
  <done>
- Placeholder iOS bundle IDs are removed
- `ios/Podfile` exists and supports standard Flutter/CocoaPods setup
- Remaining external signing values are explicitly documented
  </done>
</task>

<task type="auto">
  <name>Task 2: Add iOS review metadata for biometrics, Google Sign-In, and ads</name>
  <files>ios/Runner/Info.plist, ios/Runner/AppDelegate.swift, ios/Runner/FIREBASE_SETUP.md</files>
  <action>
Bring iOS launch metadata in line with the actual feature surface:

- Add `NSFaceIDUsageDescription` with user-facing copy that matches the biometric unlock flow.
- Add the required `CFBundleURLTypes` / callback metadata for Google Sign-In used by Drive backup authentication.
- Replace the iOS test AdMob app identifier with a production-safe launch configuration approach.
- Verify whether `google_sign_in` on the current plugin stack requires explicit `AppDelegate` URL forwarding; if yes, implement it, and if not, document why the default plugin bootstrap is sufficient.
- Capture any App Store review notes needed to explain Google Drive backup and biometric usage.

Why: Missing iOS privacy/URL metadata is a direct archive and review blocker.
  </action>
  <verify>
    <automated>cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && rg -n "NSFaceIDUsageDescription|CFBundleURLTypes|GADApplicationIdentifier" ios/Runner/Info.plist ios/Runner/AppDelegate.swift</automated>
  </verify>
  <done>
- iOS biometric prompt has required Face ID usage text
- Google Sign-In callback metadata exists for Drive backup flow
- iOS AdMob config is no longer using Google's test app ID in launch config
  </done>
</task>

<task type="auto">
  <name>Task 3: Produce iOS archive-readiness checklist</name>
  <files>ios/Runner/Info.plist, ios/Runner/AppDelegate.swift, ios/Runner/FIREBASE_SETUP.md, ios/Podfile</files>
  <action>
Create the manual iOS verification path that this Windows workspace cannot complete directly:

- Document the exact macOS/Xcode steps to run `pod install`, open `Runner.xcworkspace`, archive the app, validate signing, and confirm Google Sign-In callback behavior.
- Include checks for bundle ID, development team, provisioning, Face ID prompt text, AdMob metadata, and any required Firebase/Google service files.
- Make manual blockers explicit so "iOS ready" has a concrete definition before App Store submission.

Why: The repo can prepare iOS, but App Store readiness is not credible without a written archive checklist.
  </action>
  <verify>
    <manual>Archive Runner on macOS and verify signing, metadata, and callback behavior against the documented checklist.</manual>
  </verify>
  <done>
- iOS archive prerequisites are documented as a deterministic checklist
- Manual macOS verification path is clear and auditable
  </done>
</task>

</tasks>

<verification>
iOS launch verification:
```bash
cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && rg -n "com\\.example\\.guarden|PRODUCT_BUNDLE_IDENTIFIER|NSFaceIDUsageDescription|CFBundleURLTypes|GADApplicationIdentifier" ios
```

Manual checks:
- [ ] `pod install` succeeds on macOS
- [ ] Runner archives in Xcode with the intended bundle ID and team
- [ ] Face ID prompt text, Google Sign-In callback handling, and ad metadata are correct in the archived build
</verification>

<success_criteria>
- iOS project has a production bundle identity and reproducible dependency setup
- Face ID and Google Sign-In metadata are present before archive testing
- Remaining iOS work is reduced to explicit external signing/archive steps, not hidden repo blockers
</success_criteria>

<output>
After completion, create `.planning/phases/14-store-launch-readiness-submission-hardening/14-02-SUMMARY.md`
</output>
