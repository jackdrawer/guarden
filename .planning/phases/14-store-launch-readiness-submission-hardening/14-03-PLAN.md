---
phase: 14-store-launch-readiness-submission-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
  - 14-02
files_modified:
  - test/settings_provider_test.dart
  - test/widget_test.dart
  - lib/services/app_lifecycle_service.dart
  - lib/providers/settings_provider.dart
  - lib/main.dart
autonomous: true
requirements:
  - launch-tests

must_haves:
  truths:
    - "Launch-critical automated tests are green, especially settings initialization, lifecycle locking behavior, and app boot smoke coverage"
    - "Lifecycle timers and app bootstrap side effects are deterministic in tests and do not leak across test boundaries"
    - "Release-oriented config changes from Plans 01-02 are reflected in test-safe boot behavior"
  artifacts:
    - path: "test/settings_provider_test.dart"
      provides: "Stable coverage for settings initialization and reset behavior"
      contains: "SettingsNotifier"
    - path: "test/widget_test.dart"
      provides: "Boot smoke test that can render app shell without hanging on real services"
      contains: "GuardenApp"
    - path: "lib/services/app_lifecycle_service.dart"
      provides: "Disposable lifecycle service with test-safe timer behavior"
      contains: "dispose"
    - path: "lib/main.dart"
      provides: "App bootstrap that can be isolated or overridden in tests"
      contains: "GuardenApp"
  key_links:
    - from: "test/widget_test.dart"
      to: "lib/main.dart"
      via: "boot smoke test exercises launch bootstrap safely"
      pattern: "GuardenApp"
    - from: "test/settings_provider_test.dart"
      to: "lib/providers/settings_provider.dart"
      via: "provider initialization and reset logic are verified deterministically"
      pattern: "settingsProvider"
    - from: "lib/services/app_lifecycle_service.dart"
      to: "test/widget_test.dart"
      via: "timer lifecycle must not outlive widget tests"
      pattern: "Timer|dispose"
---

<objective>
Stabilize launch-critical automated tests so the release gate is green and reflects the hardened app bootstrap and lifecycle behavior.

Purpose: Close the red-test blocker called out in launch review and ensure platform/config changes do not leave the release gate misleading.

Output: Deterministic provider, lifecycle, and app-boot tests that pass under `flutter test`.
</objective>

<execution_context>
@C:/Users/turga/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/turga/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-store-launch-readiness-submission-hardening/14-CONTEXT.md
@.planning/phases/14-store-launch-readiness-submission-hardening/14-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make settings/provider tests deterministic</name>
  <files>test/settings_provider_test.dart, lib/providers/settings_provider.dart</files>
  <action>
Fix the current settings test instability without weakening coverage:

- Remove timing assumptions such as arbitrary small delays if provider initialization can be awaited or exposed more deterministically.
- Ensure `SettingsNotifier` initialization and reset paths are observable in tests without race conditions.
- Keep the fake service approach, but make the test assert the real launch-critical behavior: initialization, travel mode toggling, notification toggles, and reset semantics.

Why: Launch readiness requires a trustworthy baseline, not tests that pass only by timing luck.
  </action>
  <verify>
    <automated>cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && flutter test test/settings_provider_test.dart</automated>
  </verify>
  <done>
- Settings provider test no longer relies on fragile timing
- Initialization and reset behavior are asserted deterministically
  </done>
</task>

<task type="auto">
  <name>Task 2: Make lifecycle and app boot smoke tests test-safe</name>
  <files>test/widget_test.dart, lib/services/app_lifecycle_service.dart, lib/main.dart</files>
  <action>
Fix the widget/lifecycle side effects that currently leave the test suite red:

- Refactor `AppLifecycleService` so timer startup and disposal are deterministic in tests, with no leaked periodic timers after widget teardown.
- Ensure `GuardenApp` bootstrapping can render in tests without requiring real platform initialization side effects from ads, telemetry, or other launch services.
- Keep production behavior intact while allowing tests to inject or bypass launch-only side effects cleanly.

Why: Boot smoke tests should prove the app shell is renderable, not fail because background timers or plugin bootstraps leak into the test environment.
  </action>
  <verify>
    <automated>cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && flutter test test/widget_test.dart</automated>
  </verify>
  <done>
- Widget smoke test renders the app shell reliably
- Lifecycle timers are disposed or injectable in tests
- Production bootstrap remains unchanged for real users
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-green the launch baseline</name>
  <files>test/settings_provider_test.dart, test/widget_test.dart, lib/services/app_lifecycle_service.dart, lib/providers/settings_provider.dart, lib/main.dart</files>
  <action>
After fixing the targeted failures, run the whole local release gate:

- Execute `dart analyze lib test`.
- Execute `flutter test`.
- If failures remain, classify them as repo blockers versus external environment blockers and fix/document them before declaring launch readiness.

Why: The phase goal is not "some tests pass"; it is a green launch baseline.
  </action>
  <verify>
    <automated>cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && dart analyze lib test</automated>
    <automated>cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && flutter test</automated>
  </verify>
  <done>
- `dart analyze lib test` is green
- `flutter test` is green or remaining failures are explicitly externalized and documented
  </done>
</task>

</tasks>

<verification>
Launch test verification:
```bash
cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && dart analyze lib test
cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && flutter test
```

Manual checks:
- [ ] Widget tests leave no pending timers or bootstrap side effects
- [ ] Settings provider behavior under test still reflects real production initialization
</verification>

<success_criteria>
- Launch-critical tests are green and deterministic
- App lifecycle and bootstrap code are test-safe without hiding real regressions
- Release gate status is trustworthy enough to support store submission
</success_criteria>

<output>
After completion, create `.planning/phases/14-store-launch-readiness-submission-hardening/14-03-SUMMARY.md`
</output>
