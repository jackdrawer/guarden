---
phase: 05-ux-autofill
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml
  - android/app/src/main/res/mipmap-anydpi-v33/ic_launcher.xml
  - android/app/src/main/res/values/ic_launcher_background.xml
  - lib/screens/auth/login_screen.dart
  - lib/screens/onboarding/onboarding_screen.dart
  - lib/services/app_lifecycle_service.dart
autonomous: false
requirements:
  - R6
  - R3

must_haves:
  truths:
    - "Android 13+ shows monochrome themed app icon that follows system color"
    - "When biometric fails, user is smoothly redirected to master password input (not stuck)"
    - "Biometric failure shows clear error message before falling back"
    - "Onboarding mentions new features (autofill, sound/haptic settings, screenshot prevention)"
    - "Skeleton loading shimmer appears on list screens while data loads (verify existing staggered animations sufficiency)"
  artifacts:
    - path: "android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml"
      provides: "Adaptive icon for Android 8+"
    - path: "android/app/src/main/res/mipmap-anydpi-v33/ic_launcher.xml"
      provides: "Monochrome themed icon for Android 13+"
    - path: "lib/screens/auth/login_screen.dart"
      provides: "Improved biometric fallback flow"
    - path: "lib/screens/onboarding/onboarding_screen.dart"
      provides: "Updated onboarding with new feature mentions"
  key_links:
    - from: "lib/screens/auth/login_screen.dart"
      to: "lib/services/biometric_service.dart"
      via: "handles authenticate() failure gracefully"
      pattern: "biometricService.authenticate.*false.*fallback"
---

<objective>
Configure Android adaptive/themed icons, improve biometric fallback UX on login screen, and update onboarding to mention new Phase 5 features. Verify performance (skeleton/shimmer vs existing staggered animations).

Purpose: Polish the app icon for modern Android, fix biometric failure UX so users aren't stuck, and ensure new users learn about new features during onboarding.
Output: Adaptive icon config, improved login screen, updated onboarding.
</objective>

<execution_context>
@C:/Users/turga/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/turga/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ux-autofill/05-CONTEXT.md
@.planning/phases/05-ux-autofill/05-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From lib/screens/auth/login_screen.dart:
```dart
class _LoginScreenState extends ConsumerState<LoginScreen> {
  final BiometricService _biometricService = BiometricService();
  // _checkBiometricOnStart() tries biometric, on success shows SnackBar but doesn't actually unlock
  // PROBLEM: biometric success doesn't complete login flow; failure has no fallback message
  // FIX NEEDED: On biometric success, call ref.read(authProvider.notifier).biometricLogin() or similar
  // On failure, show clear message and focus password field
}
```

From lib/services/biometric_service.dart:
```dart
class BiometricService {
  Future<bool> canCheckBiometrics() async { ... }
  Future<List<BiometricType>> getAvailableBiometrics() async { ... }
  Future<bool> authenticate({String reason = '...'}) async { ... }
  // biometricOnly: false — already allows PIN/pattern fallback at OS level
}
```

From lib/screens/onboarding/onboarding_screen.dart:
```dart
// Existing onboarding — check structure and add new feature slides
```

From lib/services/app_lifecycle_service.dart:
```dart
// Existing lifecycle management — may need updates for screenshot prevention sync
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Android adaptive icon + themed monochrome icon for Android 13+</name>
  <files>android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml, android/app/src/main/res/mipmap-anydpi-v33/ic_launcher.xml, android/app/src/main/res/values/ic_launcher_background.xml</files>
  <action>
    1. **Adaptive Icon (Android 8+/API 26)** — Create or update `android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml`:
       ```xml
       <?xml version="1.0" encoding="utf-8"?>
       <adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
           <background android:drawable="@color/ic_launcher_background"/>
           <foreground android:drawable="@mipmap/ic_launcher_foreground"/>
       </adaptive-icon>
       ```
       - Create `android/app/src/main/res/values/ic_launcher_background.xml` with the brand orange color (#EF8539) or a neutral background.
       - NOTE: The foreground drawable (`ic_launcher_foreground`) needs to exist. Check if current mipmap folders have a foreground PNG. If not, use the existing `ic_launcher` as foreground or create a simple shield/lock icon vector drawable.
       - Check existing `android/app/src/main/res/mipmap-*` folders for current icon files.

    2. **Themed/Monochrome Icon (Android 13+/API 33)** — Create `android/app/src/main/res/mipmap-anydpi-v33/ic_launcher.xml`:
       ```xml
       <?xml version="1.0" encoding="utf-8"?>
       <adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
           <background android:drawable="@color/ic_launcher_background"/>
           <foreground android:drawable="@mipmap/ic_launcher_foreground"/>
           <monochrome android:drawable="@drawable/ic_launcher_monochrome"/>
       </adaptive-icon>
       ```
       - Create `android/app/src/main/res/drawable/ic_launcher_monochrome.xml` — a simple vector drawable of a shield or lock icon (Guarden brand). Use a simple path-based vector:
       ```xml
       <?xml version="1.0" encoding="utf-8"?>
       <vector xmlns:android="http://schemas.android.com/apk/res/android"
           android:width="108dp"
           android:height="108dp"
           android:viewportWidth="108"
           android:viewportHeight="108">
           <path
               android:fillColor="#000000"
               android:pathData="M54,24L30,36v18c0,16.6 10.2,32.1 24,36c13.8,-3.9 24,-19.4 24,-36V36L54,24z M54,54h18c-1.5,12.4 -8.4,23.4 -18,27.6V54H36V39l18,-9V54z"/>
       </vector>
       ```
       This creates a shield icon for monochrome mode.

    3. **Verify** existing icon structure by listing mipmap directories first. Adapt the approach based on what already exists (e.g., if there's already an adaptive icon, just add monochrome layer).
  </action>
  <verify>
    <automated>cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && ls android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml android/app/src/main/res/mipmap-anydpi-v33/ic_launcher.xml 2>&1</automated>
  </verify>
  <done>Adaptive icon XML exists for API 26+. Monochrome themed icon XML exists for API 33+. Shield vector drawable created for monochrome. Background color resource exists.</done>
</task>

<task type="auto">
  <name>Task 2: Improve biometric fallback UX on login + Update onboarding for new features</name>
  <files>lib/screens/auth/login_screen.dart, lib/screens/onboarding/onboarding_screen.dart</files>
  <action>
    1. **LoginScreen biometric fallback** — Rewrite `_checkBiometricOnStart()`:
       - When biometric fails (returns false), show a clear message: "Biyometrik doğrulama başarısız. Master şifrenizi kullanarak giriş yapabilirsiniz."
       - Auto-focus the password TextField after biometric failure (use a FocusNode).
       - When biometric succeeds: Currently shows a SnackBar but doesn't unlock. This needs fixing:
         a. After biometric success, the app should unlock. If `authProvider` has a `biometricLogin()` method, use it. If not, check if there's a way to retrieve the master key from secure storage after biometric auth.
         b. Look at authProvider for a method that unlocks without password (using stored key from secure storage after biometric gate).
         c. If no such method exists, create a simple bridge: after biometric success, read the encryption key from SecureStorage and call the appropriate unlock method on authProvider.
       - Add a "Biyometriyi Tekrar Dene" button that re-triggers biometric (currently exists as "Biyometrik Kullan" TextButton — keep it but add retry logic with error state).
       - Add a visual indicator during biometric check (loading state on the button).

    2. **OnboardingScreen** — Read existing onboarding structure, then add or update slides:
       - Add a slide for "Otomatik Doldurma" (Autofill): shield icon, brief description of autofill capability.
       - Add a slide for "Güvenlik Ayarları" (Security Settings): mention screenshot prevention, sound/haptic toggles.
       - Keep existing slides (master password setup, biometric opt-in, etc.) unchanged.
       - Style: Match existing onboarding neumorphic style. Use AppColors.of(context).
       - Claude's discretion on exact format (per CONTEXT.md).

    3. **Performance check** (R3 — skeleton loading discretion):
       - Evaluate: staggered_animations already provide list loading animation. Skeleton/shimmer adds minimal value since data loads from local Hive (near-instant).
       - Decision: Add a brief shimmer placeholder ONLY if Hive box open takes noticeable time. Otherwise, document that staggered animations + local storage make skeleton unnecessary.
       - If adding: Use a simple shimmer widget (neumorphic cards with pulsing opacity) for the first 200ms.
  </action>
  <verify>
    <automated>cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && flutter analyze lib/screens/auth/login_screen.dart lib/screens/onboarding/onboarding_screen.dart 2>&1 | tail -5</automated>
  </verify>
  <done>Login screen has clear biometric failure message with auto-focus to password field. Biometric success properly unlocks app. Onboarding includes new feature slides (autofill, security settings). Performance decision documented. All compile without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Full Phase 5 UX verification on device</name>
  <what-built>Complete Phase 5 UX overhaul: Settings toggles (haptic/sound/screenshot), empty state illustrations, form auto-save, Android autofill, adaptive icon, biometric fallback, updated onboarding.</what-built>
  <action>Human verifies all Phase 5 features on device/emulator.</action>
  <how-to-verify>
    1. Open app on Android device/emulator
    2. Check Settings screen — verify new sections: "UX & Gizlilik" (haptic, sound, screenshot toggles), "Otomatik Doldurma" (autofill enable + category checkboxes)
    3. Go to an empty list (e.g., bank accounts with no data) — verify illustration shows instead of plain text
    4. Start filling a form, navigate away without saving, return — verify draft restored with SnackBar
    5. Toggle screenshot prevention ON — verify screenshot is blocked (shows black in recent apps on Android)
    6. Test biometric on login — cancel biometric, verify fallback to password with clear message
    7. Check app icon in launcher — verify it uses adaptive icon shape
    8. If Android 13+: Check themed icon matches system color
    9. Run onboarding flow — verify new feature slides appear
    10. Test dark mode: Change system theme to dark — verify app follows automatically
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All `flutter analyze` passes
- Android adaptive icon renders correctly on API 26+
- Monochrome themed icon renders on API 33+
- Biometric failure shows clear message and focuses password field
- Biometric success unlocks the app (not just shows SnackBar)
- Onboarding has new feature slides
- Full UX flow tested end-to-end on device
</verification>

<success_criteria>
Android adaptive + monochrome icons configured. Login biometric flow handles failure gracefully with master password fallback. Onboarding updated with Phase 5 features. Human verification confirms all Phase 5 features work together on device.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-autofill/05-04-SUMMARY.md`
</output>
