---
phase: 03-core-ui
plan: 05
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - lib/screens/passwords/web_passwords_screen.dart
  - lib/screens/passwords/web_password_form_screen.dart
autonomous: false
requirements:
  - "Standart Web şifreleri özellikleri"

must_haves:
  truths:
    - "User can create, view, edit, and delete web passwords with standard password features"
    - "Web password list items show logo from LogoService or fallback letter avatar"
    - "User can copy password to clipboard from the list screen"
  artifacts:
    - path: "lib/screens/passwords/web_passwords_screen.dart"
      provides: "Web passwords list with CRUD operations and copy-to-clipboard"
      min_lines: 60
    - path: "lib/screens/passwords/web_password_form_screen.dart"
      provides: "Web password add/edit form"
      min_lines: 70
  key_links:
    - from: "lib/screens/passwords/web_passwords_screen.dart"
      to: "lib/providers/web_password_provider.dart"
      via: "CRUD operations through provider"
      pattern: "ref\\.(watch|read).*webPasswordProvider"
    - from: "lib/screens/passwords/web_passwords_screen.dart"
      to: "lib/services/logo_service.dart"
      via: "logoServiceProvider.getLogoWidget() for password logos"
      pattern: "logoServiceProvider.*getLogoWidget"
---

<objective>
Build the Web Passwords CRUD screens (list + form) with full add/edit/delete functionality, copy-to-clipboard, and Neumorphic design.

Purpose: Enable users to manage their web passwords through a polished interface with logo display and quick-copy functionality.
Output: 2 screen files replacing placeholders created in 03-03.
</objective>

<execution_context>
@C:/Users/turga/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/turga/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-core-ui/03-CONTEXT.md
@.planning/phases/03-core-ui/03-03-SUMMARY.md
@lib/theme/app_colors.dart
@lib/widgets/neumorphic/neumorphic_container.dart
@lib/widgets/neumorphic/neumorphic_button.dart
@lib/widgets/neumorphic/neumorphic_textfield.dart

<interfaces>
<!-- Key types from 03-01 and 03-02 -->

From lib/models/web_password.dart:
```dart
@HiveType(typeId: ...)
class WebPassword extends HiveObject {
  @HiveField(0) String id;
  @HiveField(1) String title;
  @HiveField(2) String? url;
  @HiveField(3) String username;
  @HiveField(4) String password;
}
```

From lib/services/logo_service.dart:
```dart
class LogoService {
  String getLogoUrl(String domain);
  Widget getLogoWidget(String domain, {double size = 48});
}
final logoServiceProvider = Provider<LogoService>(...);
```

From lib/providers/:
```dart
final webPasswordProvider = AutoDisposeNotifierProvider<WebPasswordNotifier, List<WebPassword>>(...);
// Methods: addWebPassword, updateWebPassword, deleteWebPassword
```

From lib/widgets/neumorphic/:
```dart
NeumorphicContainer({child, padding, borderRadius, width, height})
NeumorphicButton({onPressed, child, padding, color})
NeumorphicTextField({controller, hintText, obscureText, keyboardType, onChanged, prefixIcon, suffixIcon, validator})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Web Passwords list and form screens</name>
  <files>lib/screens/passwords/web_passwords_screen.dart, lib/screens/passwords/web_password_form_screen.dart</files>
  <action>
**Web Passwords List (`lib/screens/passwords/web_passwords_screen.dart`):**
Replace the placeholder with a `ConsumerWidget`. Use `Scaffold(backgroundColor: AppColors.background)`.
- AppBar: "Web Sifreleri", no elevation, AppColors.background.
- FAB: `NeumorphicButton` with "+" icon, navigates to `/passwords/add`.
- Body: `ref.watch(webPasswordProvider)` -> if empty, show centered "Henuz web sifresi eklenmedi" with an add button. Otherwise `ListView.separated`.
- Each item: `NeumorphicContainer` with `InkWell` (tap -> `/passwords/edit/${password.id}`). Row layout: LogoService `getLogoWidget(password.url ?? password.title)` on the left (48x48), Column with title (bold, textPrimary) and username partially masked (first 2 chars + "***", textSecondary), trailing: `IconButton` to copy password to clipboard (`Clipboard.setData(ClipboardData(text: password.password))`) with a SnackBar "Sifre kopyalandi".
- Swipe to delete: Wrap in `Dismissible(key: ValueKey(password.id), direction: DismissDirection.endToStart, background: red container with delete icon, confirmDismiss: show AlertDialog "Bu sifreyi silmek istediginize emin misiniz?", onDismissed: ref.read(webPasswordProvider.notifier).deleteWebPassword(password.id))`.

**Web Password Form (`lib/screens/passwords/web_password_form_screen.dart`):**
Replace the placeholder with a `ConsumerStatefulWidget`. Accept optional `String? passwordId` for edit mode.
- Use `TextEditingController`s for each field. In `initState`: if editing, find the password from provider list by ID and pre-fill controllers.
- Form fields (wrapped in `Form` widget with `GlobalKey<FormState>`):
  - `NeumorphicTextField(hintText: "Baslik", controller: titleCtrl, validator: required)`
  - `NeumorphicTextField(hintText: "URL / Domain", controller: urlCtrl)`
  - `NeumorphicTextField(hintText: "Kullanici Adi", controller: usernameCtrl, validator: required)`
  - `NeumorphicTextField(hintText: "Sifre", controller: passwordCtrl, obscureText: true, suffixIcon: visibility toggle, validator: required)`
  - `NeumorphicTextField(hintText: "Notlar", controller: notesCtrl, keyboardType: TextInputType.multiline, maxLines: 3)`
- Save button: Full-width `NeumorphicButton` with `AppColors.primaryAccent` background. On pressed: validate form, create/update `WebPassword` object, call provider's add or update method, `context.pop()`.
- AppBar title: editing ? "Sifreyi Duzenle" : "Yeni Web Sifresi".
- Dispose all TextEditingControllers in dispose().

**Common patterns:**
- Navigation: use `context.push('/path')` for going to forms, `context.pop()` to return after save.
- Empty state: Centered Column with icon, descriptive text, and NeumorphicButton to add first item.
- Form validation: title, username, password fields required (return "Bu alan zorunludur" if empty).
  </action>
  <verify>
    <automated>cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && dart analyze lib/screens/passwords</automated>
  </verify>
  <done>Web passwords list shows entries with logos, masked usernames, and copy-to-clipboard. Form supports add/edit with all fields. Swipe-to-delete works with confirmation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Core UI visually</name>
  <files>lib/screens/passwords/web_passwords_screen.dart</files>
  <action>Human verification of the complete Phase 3 Core UI implementation.</action>
  <what-built>Complete Guarden Core UI: Dashboard with budget pie chart and rotation warnings (03-03), Bank Accounts CRUD with period dropdown (03-04), Subscriptions CRUD with budget summary (03-04), Web Passwords CRUD with copy-to-clipboard (03-05), bottom navigation with 4 tabs, all in Neumorphic design.</what-built>
  <how-to-verify>
    1. Run the app: `flutter run`
    2. After login, verify the Dashboard appears with the bottom navigation bar (4 tabs: Ana Sayfa, Bankalar, Abonelikler, Sifreler).
    3. Tap "Banka Ekle" quick access button -- verify the bank form opens with a period dropdown showing 3, 6, 9, 12 month options.
    4. Fill in a bank account and save -- verify it appears in the Banks list tab with logo or letter avatar.
    5. Navigate to Subscriptions tab, add a subscription with a monthly cost -- verify the budget summary card updates and the pie chart on Dashboard reflects the new subscription.
    6. Navigate to Passwords tab, add a web password -- verify it appears in the list with masked username.
    7. Try editing and deleting entries in each section.
    8. Verify all screens have consistent Neumorphic design (soft shadows, AppColors.background color, rounded containers).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix.</resume-signal>
  <done>All Core UI screens are visually correct, CRUD operations work for all three entity types, and Neumorphic design is consistent.</done>
</task>

</tasks>

<verification>
1. `dart analyze lib/screens/passwords` passes with no errors.
2. Web password CRUD fully works: add, edit, delete with confirmation.
3. Copy-to-clipboard works with SnackBar feedback.
4. Logo widgets display for entries with URLs, fallback letter avatars for those without.
5. All screens use NeumorphicContainer, NeumorphicButton, NeumorphicTextField consistently.
</verification>

<success_criteria>
- Web passwords have standard CRUD with masked password display and copy-to-clipboard.
- All screens consistently use Neumorphic UI components and AppColors theming.
- The final checkpoint verifies the complete Phase 3 Core UI end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-ui/03-05-SUMMARY.md`
</output>
