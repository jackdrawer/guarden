---
phase: 03-core-ui
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - lib/providers/bank_account_provider.dart
  - lib/providers/subscription_provider.dart
  - lib/providers/web_password_provider.dart
  - lib/services/database_service.dart
autonomous: true
requirements: 
  - "Banka Ekranı (Esnek periyotlu 3-6-9 ay kurallı form) CRUD"
  - "Abonelikler ve Bütçe ekranı CRUD"
  - "Standart Web şifreleri özellikleri"

must_haves:
  truths:
    - "Bank Account provider allows creating, updating, deleting and listing accounts."
    - "Subscription provider handles subscriptions."
    - "Web Password provider handles standard web passwords."
    - "The Database Service is updated to properly register the new Hive TypeAdapters and open the specific boxes."
  artifacts:
    - path: "lib/providers/bank_account_provider.dart"
      provides: "CRUD ops for BankAccount"
    - path: "lib/providers/subscription_provider.dart"
      provides: "CRUD ops for Subscription"
    - path: "lib/providers/web_password_provider.dart"
      provides: "CRUD ops for WebPassword"
  key_links:
    - from: "lib/providers/bank_account_provider.dart"
      to: "lib/services/database_service.dart"
      via: "databaseService.bankAccountsBox for Hive CRUD"
      pattern: "databaseService\\.bankAccountsBox"
    - from: "lib/providers/subscription_provider.dart"
      to: "lib/services/database_service.dart"
      via: "databaseService.subscriptionsBox for Hive CRUD"
      pattern: "databaseService\\.subscriptionsBox"
    - from: "lib/providers/web_password_provider.dart"
      to: "lib/services/database_service.dart"
      via: "databaseService.webPasswordsBox for Hive CRUD"
      pattern: "databaseService\\.webPasswordsBox"
---

<objective>
To implement the CRUD operations via Riverpod AutoDisposeNotifierProviders for our custom Hive Models (BankAccount, Subscription, and WebPassword) and initialize their boxes in DatabaseService.
</objective>

<execution_context>
@C:/Users/turga/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Update DatabaseService with Type Adapters and multiple Boxes</name>
  <files>lib/services/database_service.dart</files>
  <action>
Update `DatabaseService` to call `Hive.registerAdapter` for all our new models before opening the boxes.
Change the logic to open specific boxes: `bank_accounts`, `subscriptions`, `web_passwords`. Ensure encryption key is applied to all of them.
Provide getter properties for each box, e.g., `Box<BankAccount> get bankAccountsBox => Hive.box<BankAccount>('bank_accounts');`
  </action>
  <verify>
    <automated>cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && dart analyze lib/services/database_service.dart</automated>
  </verify>
  <done>Database service correctly prepares the three distinct boxes.</done>
</task>

<task type="auto">
  <name>Task 2: Build the Providers</name>
  <files>lib/providers/bank_account_provider.dart, lib/providers/subscription_provider.dart, lib/providers/web_password_provider.dart</files>
  <action>
For each domain model, create an AutoDisposeNotifier that returns a `List<Model>`.
- `build()` reads `databaseService.XBox.values.toList()`.
- Add `addX`, `updateX`, `deleteX` methods.
- E.g: `addBankAccount(BankAccount account)` should: `box.put(account.id, account); state = box.values.toList();`
Remember to generate a random ID natively using `const Uuid().v4()` or equivalent standard if it exists, or just use `DateTime...toString()`.
  </action>
  <verify>
    <automated>cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && dart analyze lib/providers</automated>
  </verify>
  <done>All three providers (Bank, Subscription, WebPassword) are fully implemented and free of syntax errors.</done>
</task>
</tasks>

<verification>
1. `dart analyze` passes without issues in `lib/services/database_service.dart` and `lib/providers/`.
</verification>

<success_criteria>
- The CRUD controllers are successfully separated and use Riverpod state objects tightly bound to Hive boxes.
</success_criteria>
