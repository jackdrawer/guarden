---
phase: 03-core-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/models/bank_account.dart
  - lib/models/subscription.dart
  - lib/models/web_password.dart
  - lib/services/logo_service.dart
autonomous: true
requirements: 
  - "Dinamik Logo Servisi (Cache destekli Network Logolar)"
  - "Banka Ekranı (Esnek periyotlu 3-6-9 ay kurallı form) CRUD altyapısı"

must_haves:
  truths:
    - "Logo Service fetches logos from Clearbit and caches them using cached_network_image or custom caching."
    - "If logo is unavailable, a fallback method generates a letter avatar."
    - "BankAccount model supports flexible periods: 3, 6, 9, 12 months."
  artifacts:
    - path: "lib/services/logo_service.dart"
      provides: "Network logo fetching and fallback avatar generation"
      exports: ["LogoService", "logoServiceProvider"]
    - path: "lib/models/bank_account.dart"
      provides: "Hive model for bank accounts with password rotation period"
      exports: ["BankAccount"]
---

<objective>
Update domain models to support custom password rotation periods (new requirement) and implement the Dynamic Logo Service necessary for rendering Neumorphic premium avatars in the UI.
</objective>

<execution_context>
@C:/Users/turga/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Update Domain Models</name>
  <files>lib/models/bank_account.dart, lib/models/subscription.dart, lib/models/web_password.dart</files>
  <action>
Review current models in `lib/models`.
Ensure `BankAccount` has a `periodMonths` integer field. If not, add it (default 6).
Ensure all models have a `url` or `domain` field to be used by the Logo Service.
Ensure all models are properly annotated with `@HiveType()` and fields with `@HiveField()`.
If models don't exist yet (Phase 1 might have missed them), create them now.
  </action>
  <verify>
    <automated>cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && dart analyze lib/models</automated>
  </verify>
  <done>Models are verified/created and have required fields for Phase 3 features.</done>
</task>

<task type="auto">
  <name>Task 2: Implement LogoService</name>
  <files>lib/services/logo_service.dart</files>
  <action>
Create `lib/services/logo_service.dart`.
Implement a simple class `LogoService`:
- Uses the `Clearbit` API or a similar open API to fetch a logo URL based on domain.
- Provide a helper method `String getLogoUrl(String domain)` that returns `https://logo.clearbit.com/$domain`.
- Provide a `Widget getLogoWidget(String domain, {double size = 48})` that uses `CachedNetworkImage` to display the logo.
- If the image fails to load (errorWidget), fallback to a Neumorphic circular container displaying the first letter of the domain (uppercase) on a beautiful accent background.
- Include a Riverpod provider `logoServiceProvider`.
  </action>
  <verify>
    <automated>cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && dart analyze lib/services/logo_service.dart</automated>
  </verify>
  <done>LogoService is ready with Network cache fallback and Avatar generation.</done>
</task>

</tasks>

<verification>
1. All models correctly annotated for Hive.
2. LogoService compiles and passes static analysis.
3. CachedNetworkImage is utilized for caching to prevent re-fetching.
</verification>

<success_criteria>
- The app's data structures match the new feature requirements (custom periods).
- Premium UI logic for displaying company logos is available for the Dashboard and Lists.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-ui/03-01-SUMMARY.md`
</output>
