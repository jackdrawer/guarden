---
phase: 04-premium-paywall
plan: 01
type: execute
wave: 5
depends_on: ["03-05"]
files_modified:
  - lib/services/purchase_service.dart
  - lib/providers/premium_provider.dart
  - lib/screens/paywall/paywall_screen.dart
  - lib/router.dart
  - lib/screens/bank_accounts/bank_accounts_tab.dart
  - lib/screens/subscriptions/subscriptions_tab.dart
  - lib/screens/web_passwords/web_passwords_tab.dart
requirements:
  - "In-App Purchases (RevenueCat) entegrasyonu, Paywall ekran tasarımı"
  - "Freemium limitleri enforcement (5 banka, 3 abonelik, 5 web şifresi)"

must_haves:
  truths:
    - "Free users have strict creation limits: max 5 bank accounts, 3 subscriptions, 5 web passwords"
    - "When limits are hit, pressing the Add button brings up the PaywallScreen instead of the Create Form"
    - "RevenueCat is integrated directly or a mock offline premium toggle is available for test"
    - "Paywall screen has a clear design indicating benefits and offering monthly/yearly subscriptions via RC"
---

<objective>
Phase 4'ün ilk adımında Guarden'ın Freemium sınırlarını çizecek Premium/Paywall altyapısını kuracağız. Ücretsizde olan kullanıcının CRUD aksiyonlarında limitlere çarpması ve ardından şık bir Paywall ile karşılaşması sağlanacaktır. Ayrıca app içi satın alımlar için RevenueCat temeli (purchases_flutter üzerinden) entegre edilecektir. 
</objective>

<context>
@.planning/phases/04-premium-paywall/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup PurchaseService and PremiumProvider</name>
  <files>lib/services/purchase_service.dart, lib/providers/premium_provider.dart</files>
  <action>
1. Create `purchase_service.dart` wrapping the `purchases_flutter` SDK. Implement initialization (even if keys aren't ready, mock the logic). Add methods: `fetchOfferings`, `purchasePackage`, `restorePurchases`.
2. Create `premium_provider.dart` mapping customer info to a boolean `isPremium`. Use Riverpod.
3. Manage Freemium limits constants inside PremiumProvider (e.g., `maxFreeBanks = 5`, `maxFreeSubscriptions = 3`, `maxFreeWebPasswords = 5`).
4. Also create a boolean helper in the provider to dynamically check `canAddBank(currentCount)`, etc., returning false if user `!isPremium` and over the limit.
  </action>
</task>

<task type="auto">
  <name>Task 2: Build Neumorphic PaywallScreen</name>
  <files>lib/screens/paywall/paywall_screen.dart, lib/router.dart</files>
  <action>
1. Create `paywall_screen.dart`. Design a premium looking screen with Neumorphic features. Highlight benefits: Sınırsız Kasa, Seyahat Modu, Panik Modu, Zayıf Şifre Kontrolü.
2. Add Monthly and Yearly subscription cards (mock data if RC is not immediately reachable, or fetch from PurchaseService). Use `NeumorphicContainer` for plans.
3. Add a "Restore Purchases" button somewhere subtle.
4. Add `/paywall` to GoRouter configuration in `lib/router.dart`.
  </action>
</task>

<task type="auto">
  <name>Task 3: Apply Limit Enforcement (Gating) to Tabs</name>
  <files>lib/screens/bank_accounts/bank_accounts_tab.dart, lib/screens/subscriptions/subscriptions_tab.dart, lib/screens/web_passwords/web_passwords_tab.dart</files>
  <action>
1. In `bank_accounts_tab.dart`: Watch `bankAccounts.length` and `premiumProvider`. Update FAB `onPressed`: if `premiumProvider.canAddBank(length)`, `context.push('/add-bank')`, else `context.push('/paywall')`. Add a small limit counter at the top "2/5 Kasa" if not premium.
2. In `subscriptions_tab.dart`: Add limit enforcement to FAB logic the same way. Add a text limit counter.
3. In `web_passwords_tab.dart`: Add limit enforcement to FAB logic the same way. Add a text limit counter.
  </action>
</task>

</tasks>

<verification>
1. `flutter analyze` runs without errors.
2. Free users see a counter like "1/5 Kasa" on the tabs.
3. Once limits are hit, pressing "+" reliably pushes the user to the Paywall.
4. Paywall is visually premium with clear calls to action.
</verification>
