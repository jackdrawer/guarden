---
phase: 04-premium-paywall
plan: 03
type: execute
wave: 5
depends_on: ["02"]
files_modified:
  - lib/screens/bank_accounts/bank_account_detail_screen.dart
  - lib/screens/subscriptions/subscription_detail_screen.dart
  - lib/screens/web_passwords/web_password_detail_screen.dart
  - lib/screens/bank_accounts/bank_account_form_screen.dart
  - lib/screens/subscriptions/subscription_form_screen.dart
  - lib/router.dart
  - lib/services/pwned_password_service.dart
  - lib/screens/security/security_audit_screen.dart
  - lib/screens/dashboard/dashboard_tab.dart
autonomous: true
requirements:
  - "Zayıf Şifre / Pwned Passwords API Entegrasyonu (Sadece Premium)"

must_haves:
  truths:
    - "Tapping a list item navigates to a detail screen showing full credentials with copy-to-clipboard"
    - "PwnedPasswordService uses k-Anonymity (SHA-1 prefix 5 chars) to check breach status without sending full password"
    - "Security Audit screen shows all web passwords with breach/weak status, gated to Premium"
    - "Dashboard has a security card showing X weak, Y breached passwords"
    - "Bank & Subscription forms no longer require URL input; LogoService uses name-based fallback avatar"
  artifacts:
    - path: "lib/services/pwned_password_service.dart"
      provides: "k-Anonymity Pwned Passwords API check"
      exports: ["PwnedPasswordService", "pwnedPasswordProvider"]
    - path: "lib/screens/security/security_audit_screen.dart"
      provides: "Premium security audit screen"
  key_links:
    - from: "lib/screens/security/security_audit_screen.dart"
      to: "lib/services/pwned_password_service.dart"
      via: "pwnedPasswordProvider.checkPassword()"
    - from: "lib/screens/dashboard/dashboard_tab.dart"
      to: "lib/services/pwned_password_service.dart"
      via: "Security card with breach count"
---

<objective>
Detail screens (kayıt detay görüntüleme, kopyalama, düzenleme), Pwned Passwords API (k-Anonymity), güvenlik denetimi ekranı ve UX polish (URL alanlarını kaldırma) eklenecek.
</objective>

<context>
@.planning/phases/04-premium-paywall/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Detail Screens + UX Polish</name>
  <files>lib/screens/bank_accounts/bank_account_detail_screen.dart, lib/screens/subscriptions/subscription_detail_screen.dart, lib/screens/web_passwords/web_password_detail_screen.dart, lib/screens/bank_accounts/bank_account_form_screen.dart, lib/screens/subscriptions/subscription_form_screen.dart, lib/router.dart</files>
  <action>
1. Create `BankAccountDetailScreen`: Neumorphic card showing bank name, username, masked password (tap to reveal), period info, last changed date. Action buttons: Edit (push to form), Delete (confirm dialog), Copy password.
2. Create `SubscriptionDetailScreen`: Similar layout showing subscription name, cost (monthly/yearly), renewal info, credentials if any. Edit/Delete/Copy actions.
3. Create `WebPasswordDetailScreen`: Show site name, URL, username, masked password, notes. Edit/Delete/Copy actions.
4. Add GoRouter routes: `/bank/:id`, `/subscription/:id`, `/web-password/:id` parsing the id parameter.
5. Make list items in tab screens tappable, navigating to detail screens.
6. Remove `url` input field from Bank and Subscription form screens. LogoService will use name-based fallback avatar.
7. Keep `url` field in WebPasswordFormScreen (needed for site identification).
  </action>
  <verify>
    <automated>cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && dart analyze lib/screens</automated>
  </verify>
  <done>All 3 detail screens navigate correctly, list items are tappable, URL fields removed from bank/subscription forms.</done>
</task>

<task type="auto">
  <name>Task 2: PwnedPasswordService + Security Audit + Dashboard Card</name>
  <files>lib/services/pwned_password_service.dart, lib/screens/security/security_audit_screen.dart, lib/screens/dashboard/dashboard_tab.dart, lib/router.dart</files>
  <action>
1. Create `PwnedPasswordService`:
   - `checkPassword(String plainPassword)` → SHA-1 hash, take first 5 chars as prefix, query `https://api.pwnedpasswords.com/range/{prefix}`, check if remaining suffix exists in response.
   - Return `PwnedResult(isBreached: bool, count: int)`.
   - Add `pwnedPasswordProvider` Riverpod provider.
2. Create `SecurityAuditScreen`:
   - List all web passwords with breach/weak status icons.
   - "Tümünü Kontrol Et" button runs checks on all passwords.
   - Weak password check: length < 8, only digits, repeated chars.
   - Premium gate: Free users see paywall when trying to access.
   - Neumorphic design with color-coded results (green=safe, red=breached, orange=weak).
3. Update `DashboardTab`:
   - Add a Neumorphic "Güvenlik Durumu" card at the top or bottom.
   - Show summary: "X zayıf, Y ifşa şifre" with a tap action navigating to SecurityAuditScreen.
   - Premium gate on the card action (free users see paywall).
4. Add GoRouter route: `/security-audit`.
  </action>
  <verify>
    <automated>cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && dart analyze lib/services/pwned_password_service.dart lib/screens/security lib/screens/dashboard</automated>
  </verify>
  <done>Pwned API check works via k-Anonymity, security audit screen lists passwords with status, dashboard has security card.</done>
</task>

</tasks>

<verification>
1. `dart analyze` passes without errors across all new files.
2. Detail screens show credentials with copy and mask/unmask functionality.
3. Pwned check uses k-Anonymity (only 5-char SHA-1 prefix sent).
4. Security audit screen correctly color-codes breach/weak statuses.
5. Dashboard security card shows accurate counts.
6. Premium gating works on security features.
</verification>

<success_criteria>
- Users can tap any list item to see full details with copy/edit/delete actions.
- Premium users can run security audits on their passwords.
- Dashboard provides at-a-glance security overview.
- Bank/Subscription forms are simplified (no URL input needed).
</success_criteria>
