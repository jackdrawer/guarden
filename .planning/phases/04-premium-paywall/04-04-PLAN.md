---
phase: 04-premium-paywall
plan: 04
type: execute
wave: 5
depends_on: ["02"]
files_modified:
  - lib/services/notification_service.dart
  - lib/screens/home_screen.dart
  - lib/screens/settings/settings_screen.dart
  - lib/main.dart
autonomous: true
requirements:
  - "flutter_local_notifications bildirimleri (Şifre rotasyon hatırlatıcıları)"

must_haves:
  truths:
    - "NotificationService wraps flutter_local_notifications and requests permissions"
    - "On app startup, expired bank password rotations trigger a local notification"
    - "Notification timing follows 7 days + 1 day before expiry pattern"
    - "Settings screen has notification toggles (global + per-category)"
    - "Notifications are gated to Premium users"
  artifacts:
    - path: "lib/services/notification_service.dart"
      provides: "Local notification scheduling and permission handling"
      exports: ["NotificationService", "notificationServiceProvider"]
  key_links:
    - from: "lib/screens/home_screen.dart"
      to: "lib/services/notification_service.dart"
      via: "notificationServiceProvider.checkExpirations(ref) on init"
    - from: "lib/screens/settings/settings_screen.dart"
      to: "lib/services/notification_service.dart"
      via: "Notification toggle controls"
---

<objective>
flutter_local_notifications ile şifre rotasyon hatırlatıcıları, abonelik yenileme bildirimleri ve güvenlik uyarıları eklenecek. Ayarlar ekranında bildirim kontrolleri sağlanacak.
</objective>

<context>
@.planning/phases/04-premium-paywall/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NotificationService</name>
  <files>lib/services/notification_service.dart, lib/main.dart</files>
  <action>
1. Create `NotificationService` wrapping `flutter_local_notifications`:
   - `init()`: Initialize plugin with Android/iOS settings. Request notification permission.
   - `showNotification(title, body, id)`: Display an immediate notification.
   - `scheduleNotification(title, body, id, scheduledDate)`: Schedule a future notification.
   - `checkExpirations(Ref ref)`:
     - Read all BankAccounts from provider.
     - For each: calculate nextChangeDate = lastChangedAt + (periodMonths * 30 days).
     - If nextChangeDate is within 7 days: schedule "7 gün kaldı" notification.
     - If nextChangeDate is within 1 day: schedule "Yarın son gün" notification.
     - If nextChangeDate is past: show immediate "Süre doldu" notification.
   - `checkSubscriptionRenewals(Ref ref)`: Similar logic for subscription renewal dates.
   - Premium gate: Only run checks if user isPremium.
2. Add `notificationServiceProvider` Riverpod provider.
3. In `main.dart`: Initialize NotificationService after Hive and auth setup.
  </action>
  <verify>
    <automated>cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && dart analyze lib/services/notification_service.dart</automated>
  </verify>
  <done>NotificationService initializes, checks expirations, and schedules notifications.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Notifications into App Flow + Settings</name>
  <files>lib/screens/home_screen.dart, lib/screens/settings/settings_screen.dart</files>
  <action>
1. In `HomeScreen` initState or build:
   - After auth check, call `notificationServiceProvider.checkExpirations(ref)` once on app startup.
   - Only if premium + notifications enabled in settings.
2. In `SettingsScreen`:
   - Add a "Bildirimler" section with toggles:
     - Global aç/kapa switch.
     - "Banka Şifre Rotasyonu" toggle.
     - "Abonelik Yenileme" toggle.
     - "Güvenlik Uyarıları" toggle.
   - Store toggle states in SettingsService (Hive box).
   - Premium gate: Non-premium users see toggles disabled with paywall prompt.
  </action>
  <verify>
    <automated>cd "c:/Users/turga/Desktop/Guarden PW Manager/guarden" && dart analyze lib/screens/home_screen.dart lib/screens/settings/settings_screen.dart</automated>
  </verify>
  <done>App startup triggers notification checks, settings screen has working notification toggles.</done>
</task>

</tasks>

<verification>
1. `dart analyze` passes without errors.
2. Premium users receive rotation notifications on app startup.
3. Settings toggles correctly enable/disable notification categories.
4. Free users see disabled notification toggles with paywall prompt.
</verification>

<success_criteria>
- Notification system is functional for password rotation reminders.
- Users have granular control over notification categories in Settings.
- Premium gating ensures notifications are a premium-only feature.
</success_criteria>
